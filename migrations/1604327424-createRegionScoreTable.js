exports.up = async (sql) => {
  await sql`
  CREATE TABLE IF NOT EXISTS region_scores (
    region_scores_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    answered_questions INTEGER DEFAULT 0,
		correct_questions INTEGER  DEFAULT 0,
		region_id integer NOT NULL REFERENCES regions (region_id) ON DELETE CASCADE ON UPDATE CASCADE,
		user_id integer NOT NULL REFERENCES users (user_id) ON DELETE CASCADE ON UPDATE CASCADE);

  `;
};

exports.down = async (sql) => {
  await sql`
  DROP TABLE IF EXISTS region_scores;`;
};
