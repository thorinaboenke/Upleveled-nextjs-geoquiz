exports.up = async (sql) => {
  await sql`
  CREATE TABLE IF NOT EXISTS category_scores (
    category_scores_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    answered_questions INTEGER DEFAULT 0,
		correct_questions INTEGER  DEFAULT 0,
		category_id integer NOT NULL REFERENCES categories (category_id) ON DELETE CASCADE ON UPDATE CASCADE,
		user_id integer NOT NULL REFERENCES users (user_id) ON DELETE CASCADE ON UPDATE CASCADE);

  `;
};

exports.down = async (sql) => {
  await sql`
  DROP TABLE IF EXISTS category_scores;`;
};
